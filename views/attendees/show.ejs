<% include ../partials/header %>

<div class="container">
<h1 class="content">This is <%= attendee.name %>s page</h1>

				    		<%var keys = Object.keys(attendee.toJSON());%>
				    		<%keys.shift()%>
				    		<%keys.shift()%>
				    		<%keys.pop()%>

							<% keys.forEach(function(key){ %>
								<h2>Number of <%= key %>: <%= attendee[key] %></h2>
							<% }) %>
<!-- <h2>Total units: <%= attendee.beers + attendee.wine + attendee.liquor %></h2> -->



</div>

<!-- Här kan man ha massa kul stats -->

<% include ../partials/footer %>